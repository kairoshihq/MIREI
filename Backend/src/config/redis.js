// backend/src/config/redis.js (optional, untuk cache nanti)
// Sementara kosong dulu karena ENABLE_CACHE=false

export const getRedisClient = () => {
  return null; // Redis disabled untuk alpha phase
};